# 测试用 bootstrap：覆盖主 bootstrap.yml，禁用 Nacos —— @SpringBootTest（StockTccIT）离线运行，
# 数据源 / Redis 由 Testcontainers 经 @DynamicPropertySource 注入。
spring:
  application:
    name: shopsphere-product
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
